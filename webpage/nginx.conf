events {
  worker_connections  4096;  ## Default: 1024
}

http {
	server {
		listen 80;
		#site
		location / {
			root /var/www;
			try_files $uri $uri/ =404;
		}
		#front end docs
		#location /frontdocs {
		#	root /code/frontdocs
		#	try_files $uri $uri/ =404
		#}
	}
}
